# V1_00 (RC1, PS4 disc)

Per-template overrides for the PS4 disc build where its layout differs from the base
(rc1-branch) set. The base set stays untouched as the universal per-template fallback;
only structs proven different against the disc psarcs get a copy here.
Namespace: libMBIN.V1_00.Structs.
